<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Bles</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f5f5f5;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
      }

      .login-container {
        display: flex;
        align-items: center;
        justify-content: center;
        flex-direction: column;
        height: 50%;
        min-height: 300px;
        width: 30%;
        background-color: #fff;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
        border-radius: 5px;
        padding: 20px;
        text-align: center;
      }

      h1 {
        font-size: 24px;
        margin-bottom: 20px;
      }

      .google-login-button {
        background-color: #4285f4;
        color: #fff;
        border: none;
        border-radius: 5px;
        padding: 10px 20px;
        font-size: 18px;
        cursor: pointer;
        transition: background-color 0.3s;
      }

      .google-login-button:hover {
        background-color: #357ae8;
      }

      /* Style the form container */
      .form-container {
        display: flex;
        flex-direction: column;
        align-items: center;
        width: 300px;
        margin: 0 auto;
        padding: 20px;
      }

      .form-container label {
        padding: 7px;
      }

      /* Style the input fields */
      .form-container input {
        width: 100%;
        padding: 15px;
        margin: 5px 0;
        border: 1px solid #ccc;
        border-radius: 3px;
      }

      /* Style the submit button */
      .form-container button {
        width: 100%;
        padding: 10px;
        background-color: #007bff;
        color: #fff;
        border: none;
        border-radius: 3px;
        cursor: pointer;
        transition: background-color 0.3s ease;
      }

      /* Style the submit button on hover */
      .form-container button:hover {
        background-color: #0056b3;
      }
      .nav-container {
        display: flex;
        flex-direction: row;
      }

      .nav-container ul {
        list-style: none;
      }

      .nav-container a {
        text-decoration: none;
      }
    </style>
  </head>
  <body>
    <div class="login-container">
      <h1>Bles</h1>
      <h3>Join Us Today!</h3>
      <!-- <form method="POST" class="form-container">
        <label for="email">Email</label>
        <input type="email" placeholder="email" name="email" />
        <label for="password">Password</label>
        <input type="password" placeholder="password" name="password" />
        <button type="submit">login</button>
      </form> -->
      <p style="padding: 5px">Sign in with your Stripe account:</p>
      <a href="stripe" class="google-login-button" style="text-decoration: none"
        >Login with Stripe</a
      >
      <strong style="margin: 5px; padding: 5px">
        <a href="/" style="text-decoration: none; color: #0275d8"> Return </a>
      </strong>
      <!-- <div class="nav-container">
        <ul>
          <li><a href="/">Return</a></li>
          <li><a href="/auth/register">Register</a></li>
        </ul>
      </div> -->
    </div>
  </body>
</html>
